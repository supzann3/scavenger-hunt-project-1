<div>
  <% @lists.each do |list| %>
    <% if list.clues.length>0 %>
    <%= list.name %>
    <%= button_to "Play", "/lists/#{list.id}/clues/#{list.clues.first.id}", class: "btn btn-lg btn-success" %>
    <% end %>
  <% end %>

</div>
